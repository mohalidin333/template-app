import { LucideIcon } from "lucide-react";

export type BackupRestoreData = {
  tableName: string;
  dataSize: string;
};

export type CardItems = {
  title: string;
  icon: LucideIcon;
  value: string;
};
